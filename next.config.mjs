/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['themexriver.com',"i.ibb.co",'i.pinimg.com'], // Add the domain where your image is hosted
    },
};

export default nextConfig;

